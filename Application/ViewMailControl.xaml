<UserControl x:Class="EmailApplication.ViewMailControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EmailApplication"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <!-- Outer Container -->
    <Grid>
        <DockPanel>
            <!-- Navigation Bar -->
            <Border DockPanel.Dock="Top" Background="White" BorderBrush="#CCCCCC" BorderThickness="1" Padding="0, 4">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Left Side -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <TextBlock Text="Email Application " FontWeight="Bold" FontSize="16"/>
                        <Border Width="2" Height="24" Background="#CCCCCC" Margin="8,0"/>
                        <Button Content="←" Width="32" Click="BackButton_Click"/>
                    </StackPanel>

                    <!-- Right Side -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button Content="R" Width="16" Margin="8, 0"/>
                        <Button Content="↑" Width="16" Margin="8, 0"/>
                        <Button Content="↓" Width="16" Margin="8, 0"/>
                        <Button Content="D" Width="16" Margin="8, 0"/>
                        <Button Content=". . ." Width="24" Margin="8, 0"/>
                        <Border Width="2" Height="24" Background="#CCCCCC" Margin="8,0"/>
                        <Button Margin="4, 0" Padding="4, 0">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Border Width="24" Height="24" CornerRadius="12" Background="#5B9BD5" Margin="0,0,6,0">
                                    <TextBlock Text="A" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"/>
                                </Border>
                                <TextBlock Text="Account" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
            <!-- Email Display Box -->
            <Border Background="White" BorderBrush="#CCCCCC" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <!-- Subject Bar -->
                    <Border Grid.Row="0" Background="White" Padding="0, 2">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Subject}" FontWeight="Bold" FontSize="16" Margin="8,0"/>
                            <TextBlock Grid.Column="1" Text="{Binding DateSent}" Foreground="#888888" HorizontalAlignment="Right"/>
                            <Button Grid.Column="2"  Content="Mark"/>
                        </Grid>
                    </Border>

                    <!-- Sender and Recipients -->
                    <Border Grid.Row="1" Background="White" Padding="0,2">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Account Picture -->
                            <Border Grid.Row ="0" Grid.Column="0" Grid.RowSpan="2" Width="24" Height="24" CornerRadius="12" Background="#5B9BD5" Margin="0,0,6,0">
                                <TextBlock Text="A" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"/>
                            </Border>

                            <!-- Sender -->
                            <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal">
                                <TextBlock Grid.Row="0" Grid.Column="1" Text="Sender: " FontWeight="Bold" Width="80"/>
                                <TextBlock Text="{Binding Sender}"/>
                            </StackPanel>

                            <!-- Recipients -->
                            <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal">
                                <TextBlock Text="To: " FontWeight="Bold" Width="80"/>
                                <ItemsControl ItemsSource="{Binding Recipients}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding}" Margin="0, 0, 8, 0"/>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Body -->
                    <Border Grid.Row="3">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="10">
                            <TextBox Text="{Binding Body}" IsReadOnly="True" BorderThickness="0" TextWrapping="Wrap"/>
                        </ScrollViewer>
                    </Border>

                    <!-- Files Attached -->
                    <Border Grid.Row="4" BorderBrush="#CCCCCC" BorderThickness="1">
                        <TextBlock Text="File(s) Attached:"/>
                    </Border>
                </Grid>
            </Border>
        </DockPanel>
    </Grid>
</UserControl>